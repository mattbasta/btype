object Foo {
    int:x;
    new() {
        self.x = 0;
    }
    final int:getX() {
        # `getXSecret` is not final, so it cannot be called from a final
        # method.
        return self.getXSecret();
    }
    int:getXSecret() {
        return 42;
    }
}

func main() {
    var foo = new Foo();
    var bar = foo.getX();
}
export main;
