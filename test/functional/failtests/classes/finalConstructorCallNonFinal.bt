object Foo {
    int:x;
    final new() {
        # `x` is final, so it can only be set from the constructor
        # `getX` is not final, so this should fail.
        self.x = self.getX();
    }
    int:getX() {
        return 42;
    }
}

func main() {
    var foo = new Foo();
}
export main;
